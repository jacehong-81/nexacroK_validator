export const nexacroParser = {
    parser(input: string)
    {
        // 향후 실제 파싱 로직으로 대체 예정
        return {
            type: "stub",
            input,
            result: "parsed content (stub)"
        };
    }

};
